const INITIAL_STATE = {
    authenticated: "",
    errorMessage: ""
}


const authReducer = (state = INITIAL_STATE, action) => {
    switch (action) {
        default:
            return state;
    }
};

export default authReducer;